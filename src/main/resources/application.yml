#spring.application.name=Spring_Data_JPA_Homework
spring:
  application:
    name: Spring_Data_JPA_Homework
  datasource:
    url: jdbc:postgresql://localhost:5432/homework_db
    username: postgres
    password: database09
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show_sql: true